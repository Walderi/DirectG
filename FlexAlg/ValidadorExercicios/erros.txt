%locations
%error-verbose
%%
programa:



inicio: T_INICIO enter
|{erros++; yyerror("esperado INICIO",yylineno, yytext);};

int yyerror(char *s, int line,char *msg)
{
	printf("ERROR->%d %s %s\n", line, s, msg);
	return 0;
}

colocar por default os valores das variaveis de certos tipos
declaracao de variaveis inserido numa tabela Hash com o nome da variavel, tipo, escopo,valor default
Repetição 15, 17, 18, 20, 21
String 2, 5
